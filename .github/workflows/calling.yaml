name: Call workflow

on:
  push:

jobs:
  main:
    uses: ./.github/workflows/reusable.yaml
    
  results:
    name: results
    runs-on: ubuntu-latest
    needs:
      - main
    steps:
      - name: results
        run: |
          echo "the result of step is ${{ needs.main.result }}"
