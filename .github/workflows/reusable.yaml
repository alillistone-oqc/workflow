name: Reusable workflow

on:
  workflow_call:

jobs:
  job1:
    name: First job
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        id: world
        run: |
          echo "hello world"
      - name: Hello mars
        id: mars
        continue-on-error: true
        run: |
          echo "hello mars"
          exit 1
